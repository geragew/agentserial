# Permission-stale scenario

This scenario is intentionally not encoded in v0.1. Capability lifetime,
revocation semantics, external authorization decisions, and trusted causal
ordering cannot be represented faithfully by the current atomic resource model.
Presenting a verdict would overstate the checker's knowledge.

